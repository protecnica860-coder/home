import { motion } from "framer-motion";
import { useInView } from "framer-motion";
import { useRef } from "react";
import {
  Settings,
  FileEdit,
  ToggleLeft,
  Users,
  Workflow,
  BarChart,
  FileText,
  CheckCircle2,
} from "lucide-react";
import { Button } from "@/components/ui/button";

const configurables = [
  { icon: FileEdit, label: "Formularios personalizados" },
  { icon: ToggleLeft, label: "Estados y flujos de trabajo" },
  { icon: FileText, label: "Campos adicionales" },
  { icon: Users, label: "Roles y permisos" },
  { icon: Workflow, label: "Automatizaciones" },
  { icon: BarChart, label: "Reportes a medida" },
];

export const Adaptation = () => {
  const ref = useRef(null);
  const isInView = useInView(ref, { once: true, margin: "-100px" });

  return (
    <section
      id="adaptacion"
      className="section-padding bg-gradient-to-b from-secondary/30 via-background to-background"
    >
      <div className="container-custom" ref={ref}>
        <div className="grid lg:grid-cols-2 gap-12 lg:gap-20 items-center">
          {/* Left - Content */}
          <motion.div
            initial={{ opacity: 0, x: -30 }}
            animate={isInView ? { opacity: 1, x: 0 } : {}}
            transition={{ duration: 0.6 }}
          >
            <div className="inline-flex items-center gap-2 px-4 py-2 bg-primary/10 rounded-full text-primary text-sm font-medium mb-6">
              <Settings className="w-4 h-4" />
              Adaptación total
            </div>

            <h2 className="text-3xl md:text-4xl lg:text-5xl font-bold text-foreground mb-6 leading-tight">
              Se adapta a{" "}
              <span className="gradient-text">tu operación</span>
            </h2>

            <p className="text-lg text-muted-foreground mb-6">
              No te vendemos una plantilla genérica. Lo revisamos contigo,
              entendemos tu proceso, y lo adaptamos a tu realidad.
            </p>

            <div className="bg-card rounded-xl border border-border p-6 mb-8">
              <p className="text-foreground font-medium mb-4">
                ¿Qué podemos configurar?
              </p>
              <div className="grid grid-cols-2 gap-3">
                {configurables.map((item, i) => (
                  <motion.div
                    key={i}
                    initial={{ opacity: 0, y: 10 }}
                    animate={isInView ? { opacity: 1, y: 0 } : {}}
                    transition={{ delay: 0.3 + i * 0.08 }}
                    className="flex items-center gap-2 text-sm text-muted-foreground"
                  >
                    <item.icon className="w-4 h-4 text-primary" />
                    {item.label}
                  </motion.div>
                ))}
              </div>
            </div>

            <Button size="lg" className="btn-glow shadow-lg">
              Quiero que lo adapten a mi empresa
            </Button>
          </motion.div>

          {/* Right - Visual */}
          <motion.div
            initial={{ opacity: 0, x: 30 }}
            animate={isInView ? { opacity: 1, x: 0 } : {}}
            transition={{ duration: 0.6, delay: 0.2 }}
            className="relative"
          >
            <div className="bg-card rounded-2xl border border-border shadow-xl overflow-hidden">
              {/* Header */}
              <div className="bg-primary/5 border-b border-border px-6 py-4">
                <h3 className="font-semibold text-foreground">
                  Configuración de tu empresa
                </h3>
              </div>

              {/* Content */}
              <div className="p-6 space-y-4">
                {[
                  {
                    label: "Nombre del flujo",
                    value: "Servicio Técnico",
                    active: true,
                  },
                  {
                    label: "Estados personalizados",
                    value: "Pendiente → En proceso → Cerrado",
                    active: true,
                  },
                  {
                    label: "Checklist técnico",
                    value: "6 ítems configurados",
                    active: true,
                  },
                  {
                    label: "Campos adicionales",
                    value: "Número de serie, Garantía",
                    active: true,
                  },
                  {
                    label: "Notificaciones",
                    value: "WhatsApp + Email",
                    active: true,
                  },
                ].map((item, i) => (
                  <motion.div
                    key={i}
                    initial={{ opacity: 0, x: 20 }}
                    animate={isInView ? { opacity: 1, x: 0 } : {}}
                    transition={{ delay: 0.4 + i * 0.1 }}
                    className="flex items-center justify-between p-4 bg-background rounded-lg border border-border/50"
                  >
                    <div>
                      <p className="text-xs text-muted-foreground mb-1">
                        {item.label}
                      </p>
                      <p className="text-sm font-medium text-foreground">
                        {item.value}
                      </p>
                    </div>
                    <CheckCircle2 className="w-5 h-5 text-green-500" />
                  </motion.div>
                ))}
              </div>
            </div>

            {/* Badge */}
            <motion.div
              initial={{ opacity: 0, scale: 0.8 }}
              animate={isInView ? { opacity: 1, scale: 1 } : {}}
              transition={{ delay: 1 }}
              className="absolute -top-3 -right-3 bg-primary text-primary-foreground px-4 py-2 rounded-lg shadow-lg text-sm font-medium"
            >
              100% adaptable
            </motion.div>
          </motion.div>
        </div>
      </div>
    </section>
  );
};
